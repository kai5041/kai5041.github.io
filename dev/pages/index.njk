---
title: home
layout: layout.njk
---

<style>
  :root {
    --radius: 14px;
  }

  main {
    max-width: 80rem;
    margin-inline: auto;
    padding: 3rem 1.5rem;
  }

  .hero {
    width: 100%;
    padding: 3rem 2rem;
    font-size: 1.5rem;
    color: var(--bgc);
    background: var(--fgc);
    border-radius: var(--radius);
  }

  .hero div {
    max-width: 70rem;
    margin-inline: auto;
    text-align: center;
  }

  .hero a {
    color: var(--bgc);
    text-underline-offset: 3px;
  }

  .hero p {
    margin: 0.4rem 0;
  }

  section:last-of-type {
    display: grid;
    gap: 1.5rem;
  }

  .offer {
    border: 1px solid var(--fgc);
    border-radius: var(--radius);
    padding: 1.75rem 1.5rem;
  }

  .offer-title {
    margin: 0 0 0.5rem;
    font-size: 1.2rem;
  }

  .offer p {
    margin: 0.5rem 0;
    line-height: 1.5;
  }


  .price {
    margin-top: 0.75rem;
    padding-top: 0.75rem;

    font-size: 1.2rem;
    opacity: 0.85;

    border-top: 1px solid var(--fgc);
  }

  section p a {
    text-decoration: none;
    border-bottom: 1px solid currentColor;
  }

  .small {
    font-size: 1rem;
  }

  @media (max-width: 700px) {
    main {
      padding: 2rem 1rem;
    }

    .hero {
      padding: 2rem 1.25rem;
      font-size: 1.25rem;
    }

    .offer {
      padding: 1.25rem;
    }
  }
</style>

<main>

  <section class="hero">
    <div>
      <p><strong><em>{{ index.hero | t(lang) | safe }}</em></strong></p>
    </div>
  </section>

  <section>
    {% for offer in index.offers | t(lang) %}
    <div class="offer">

      <h3 class="offer-title">❖ {{ offer.title }}</h3>

      <p>{{ offer.description | safe }}</p>

      <p class="price">{{ offer.pricing | safe }}</p>

      <a class="cta" href="/{{ lang }}/services/{{ offer.link }}/">▸ {{ index.learn_more | t(lang) }}</a>

    </div>
    {% endfor %}
  </section>

  {% note global.note, lang, index.currency_note %}

  <!--
  <section>
    <h3>{{ index.lr | t(lang) }}</h3>
    <p>{{ global.coming_soon | t(lang) }}</p>
  </section>
  -->

</main>
